# Chain
MONAD_RPC_URL=https://rpc.monad.xyz

# Database
DATABASE_URL=mysql://root:@localhost:3306/nadfun_limit_orders

# Agent
AGENT_PORT=3001
MONITOR_INTERVAL_MS=5000
TX_DEADLINE_SECONDS=300

# Frontend
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id
NEXT_PUBLIC_AGENT_URL=http://localhost:3001

# Security — REQUIRED: generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AGENT_ENCRYPTION_KEY=

# AI keys are configured by each user via BYOK in the Settings page (no server-side defaults)
